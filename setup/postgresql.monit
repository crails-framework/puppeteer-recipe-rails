source ./variables

cat << EOF
check process postgresql with pidfile $psql_pid
  start program = "$psql_service start" as uid postgres and as gid postgres
  stop  program = "$psql_service stop"  as uid postgres and as gid postgres
  if cpu > 80% for 5 cycles then restart
  if totalmem > 600Mb then alert
  group database
EOF
